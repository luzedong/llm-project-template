# 核心依赖
requests>=2.31.0
loguru>=0.7.0
tqdm>=4.65.0

# LLM Providers（可选，根据使用的 Provider 安装）
# OpenAI
openai>=1.0.0

# Anthropic (Claude)
anthropic>=0.25.0

# 数据处理（可选）
# pandas>=2.0.0  # 如果需要处理 CSV 文件，取消注释

# 环境变量（可选）
# python-dotenv>=1.0.0  # 如果需要从 .env 文件加载配置，取消注释
